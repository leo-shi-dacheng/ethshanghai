{
  "name": "rwa-debt-workshop",
  "version": "1.0.0",
  "description": "A Hardhat project for a workshop on tokenizing debt assets.",
  "scripts": {
    "test": "hardhat test",
    "demo:kyc": "hardhat run --network localhost scripts/deploy-erc3643-kyc.js",
    "demo:whitelist": "hardhat run --network localhost scripts/deploy-whitelist-fallback.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
